<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dieg.com.daggereg">

    <application
        android:name=".SportsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".constructor_injection.ConstructorInjectActivity" />
        <activity android:name=".field_injection.FieldInjectActivity" />
        <activity android:name=".method_injection.MethodInjectActivity" />
        <activity android:name=".modules_provider_method.ModuleProviderActivity"></activity>
    </application>

</manifest>